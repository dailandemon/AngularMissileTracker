


export const environment = {
  production: false,
  googleMapsApiKey: 'Google maps token',  // ← get one from Google Cloud Console
  apiBaseUrl: 'http://localhost:5208',           // ← your API base URL

  jwtToken: 'Bearer [authorization token]', // ← your JWT token
};
